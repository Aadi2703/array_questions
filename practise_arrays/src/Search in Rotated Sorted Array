class Solution {
    public int search(int[] nums, int target) {
        for(int i = 0 ; i <nums.length-1;i++){    //leatcode number 33
            if(nums[i] == target){
               return i;
            }
        }
        return -1;
    }
}
